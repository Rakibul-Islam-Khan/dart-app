import React from 'react';
import {View, StyleSheet} from 'react-native';

const City = () => {
    return (
        <View>
            
        </View>
    );
}

const styles = StyleSheet.create({})

export default City;
